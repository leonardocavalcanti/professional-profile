spring:
  data:
    mongodb:
      host: skills-mongodb
      username: user
      password: ${MONGODB_PASSWORD}
      database: professional-profile
      port: 27017

server:
  context-path: /skills
  port: 7000
